{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": "src", // Set the base directory to "src"
    "ignoreDeprecations": "6.0",
    "paths": {
      "@schemas/*": ["schemas/*"] // Set up a path alias for schemas
    }
  }
}
